name: Build APK
on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Android AIDL and Build Tools
        run: |
          sudo apt update
          sudo apt install -y openjdk-17-jdk python3-pip
          # Install the specific Android SDK component containing AIDL
          mkdir -p ~/.android && touch ~/.android/repositories.cfg
          wget https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip commandlinetools-linux-9477386_latest.zip
          mkdir -p cmdline-tools/latest
          mv cmdline-tools/bin cmdline-tools/latest/
          mv cmdline-tools/lib cmdline-tools/latest/
          export PATH=$PATH:$(pwd)/cmdline-tools/latest/bin
          yes | ./cmdline-tools/latest/bin/sdkmanager --sdk_root=$HOME/android-sdk "build-tools;34.0.0" "platforms;android-34" "platform-tools"

      - name: Build with Buildozer
        working-directory: chatbot
        run: |
          pip3 install --user --upgrade buildozer
          export PATH=$PATH:$HOME/.local/bin
          # Point Buildozer to the SDK we just manually updated
          export ANDROID_SDK_HOME=$HOME/android-sdk
          buildozer android debug
